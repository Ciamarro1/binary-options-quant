{
  "name": "binary-options-quant",
  "version": "0.1.0",
  "description": "Binary options quantitative system",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "lint": "echo \"No linting configured yet\" && exit 0",
    "build": "echo \"No build step required\" && exit 0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
